const header=document.querySelector('#header');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>30),{passive:true});
const menuBtn=document.querySelector('#menuBtn');menuBtn.addEventListener('click',()=>{const open=document.body.classList.toggle('menu-open');menuBtn.setAttribute('aria-expanded',open)});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>document.body.classList.remove('menu-open')));
const searchPanel=document.querySelector('#searchPanel');const toggleSearch=open=>{searchPanel.classList.toggle('open',open);searchPanel.setAttribute('aria-hidden',!open);if(open)setTimeout(()=>searchPanel.querySelector('input').focus(),250)};document.querySelector('#searchBtn').addEventListener('click',()=>toggleSearch(true));document.querySelector('#closeSearch').addEventListener('click',()=>toggleSearch(false));addEventListener('keydown',event=>{if(event.key==='Escape')toggleSearch(false)});
